{{ resource.description_time() }} {{ resource.description_tabletype() }} {{ resource.meta.description_summary|default("", true) }}

Processing layer:
  {{ resource.description_layer() }} {{ resource.meta.description_layer|default("", true) }}

Data source:
  {{ resource.description_datasource() }} {{ resource.meta.description_datasource|default("", true) }}

Primary key:
  {{ resource.description_primarykey() }} {{ resource.meta.description_primarykey|default("", true) }}

Usage Warnings
^^^^^^^^^^^^^^

{% for uw in resource.meta.usage_warnings %}
{% if uw is mapping %}* {{ uw.text }}{% else %}* {{ warnings[uw] }}{% endif -%}
{% endfor %}
{% if resource.meta.sources|length > 1 %}* {{ warnings.multiple_inputs }}{% endif %}
{%- if "ferc" in resource.name %}* {{ warnings.ferc_is_hard }}{% endif %}

{% if resource.meta.description_details %}
Additional Details
^^^^^^^^^^^^^^^^^^

{{ resource.meta.description_details }}
{% endif %}
