---
name: docker-build-test
on:
  workflow_dispatch:
  pull_request:
  merge_group:
    types:
      - checks_requested

jobs:
  pudl_docker_build:
    name: Test building the PUDL ETL Docker image
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: succeed
        run: exit 0
