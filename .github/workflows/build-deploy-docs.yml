name: build-deploy-docs

on:
  push:
    branches:
      - main
  workflow_dispatch:

permissions:
  contents: write

env:
  PUDL_OUTPUT: /home/runner/pudl-work/output/
  PUDL_INPUT: /home/runner/pudl-work/input/
  DAGSTER_HOME: /home/runner/pudl-work/dagster_home/

jobs:
  docs:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash -l {0}
    if: contains('refs/heads/main refs/heads/stable refs/heads/nightly', github.ref) || contains(github.ref, 'refs/heads/v20')
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install pudl environment with pixi
        uses: prefix-dev/setup-pixi@v0.9.5
        with:
          locked: true
          cache: true
          cache-write: ${{ github.event_name == 'push' && github.ref_name == 'main' }}

      - name: Make input, output and dagster dirs
        run: mkdir -p ${{ env.PUDL_OUTPUT }} ${{ env.PUDL_INPUT}} ${{ env.DAGSTER_HOME }}

      - name: Lint and build PUDL documentation with Sphinx
        run: |
          pixi run docs-build

      - name: Compute destination dir
        run: |
          DEST_DIR=${{ case(github.ref == 'refs/heads/main', 'latest', github.ref) }}
          echo DEST_DIR=${DEST_DIR#refs/heads/} > "$GITHUB_ENV"

      - name: Deploy to GitHub Pages at the appropriate version path
        uses: peaceiris/actions-gh-pages@v4
        with:
          publish_branch: gh-pages
          github_token: ${{ secrets.GITHUB_TOKEN }}
          publish_dir: docs/_build/html
          destination_dir: en/${{ env.DEST_DIR }}
          user_name: "pudlbot"
          user_email: "pudl@catalyst.coop"
