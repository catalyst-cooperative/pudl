version: 2
sources:
  - name: pudl
    tables:
      - name: core_ferc714__yearly_planning_area_demand_forecast
        data_tests:
          - foreign_key:
              arguments:
                fk_column_names:
                  - respondent_id_ferc714
                pk_table_name: source('pudl', 'core_pudl__assn_ferc714_pudl_respondents')
                pk_column_names:
                  - respondent_id_ferc714
          - expect_columns_not_all_null
          - check_row_counts_per_partition:
              arguments:
                partition_expr: report_year
        columns:
          - name: respondent_id_ferc714
          - name: report_year
          - name: forecast_year
          - name: summer_peak_demand_forecast_mw
          - name: winter_peak_demand_forecast_mw
          - name: net_demand_forecast_mwh
