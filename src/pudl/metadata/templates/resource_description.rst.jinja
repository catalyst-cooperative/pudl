{{ descriptions.summary.description|safe }}

{% if descriptions.availability.type == "True" %}
Most-recent data:
  {{ descriptions.availability.description|safe }}
{% endif %}

Processing:
  {{ descriptions.layer.description|safe }}

Source:
  {{ descriptions.source.description|safe }}

Primary key:
  {{ descriptions.primary_key.description|safe }}

{% if descriptions.usage_warnings|length > 0 %}
Usage Warnings
^^^^^^^^^^^^^^

{% for uw in descriptions.usage_warnings %}
* {{ uw.description|safe }}
{% endfor %}
{% endif %}

{% if descriptions.details.type == "True" %}

Additional Details
^^^^^^^^^^^^^^^^^^

{{ descriptions.details.description|safe }}
{% endif %}
